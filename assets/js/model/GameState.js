define(function() {
    var GameState = function() {
        this.QUEUE = 'que';
        this.SETUP = 'setup';
        this.STARTED = 'started';
        this.DONE = 'done';
    };

    return GameState;
});
