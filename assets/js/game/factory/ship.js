define([''], function() {
    var ShipFactory = function() {

    };

    ShipFactory.prototype.create = function(shipType) {
        if(shipType === 'Destoryer') {

        }
    };

    return new ShipFactory();
});