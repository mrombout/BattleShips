define(function() {
    var BoardFactory = function() {

    };

    BoardFactory.prototype.create = function(board) {
        // TODO Create a new Board3D with the appropriate ships on it, if any
    };

    return new BoardFactory();
});